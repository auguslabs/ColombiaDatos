# Artefactos de modelos entrenados

Generados por `src/model_training.py`:

- `advanced_ensemble.pkl` — Random Forest / Gradient Boosting
- `clustering_segments.pkl` — Modelo de segmentación no supervisado

Los archivos `.pkl` se excluyen del control de versiones por defecto (ver `.gitignore`).
Usar Git LFS si se requiere versionarlos.
